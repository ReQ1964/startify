---
import UniversalWrapper from '@/components/Layout/UniversalWrapper.astro';
import Summary from './Summary.astro';
import { getCollection } from 'astro:content';
import type { CollectionEntry } from 'astro:content';
import Summary2 from './Summary2.astro';

const allSummaries = await getCollection('packagesSummaries');
type summaryType = CollectionEntry<'packagesSummaries'>;
---

<UniversalWrapper class="summariesList">
  {allSummaries.map((summary: summaryType) => <Summary summary={summary} />)}
  {allSummaries.map((summary: summaryType) => <Summary2 summary={summary} />)}
</UniversalWrapper>

<style lang="scss">
  .summariesList {
    display: flex;
    flex-wrap: wrap;
    gap: var(--content-blocks-gap-mobile);
    margin-bottom: 2rem;
  }
</style>
