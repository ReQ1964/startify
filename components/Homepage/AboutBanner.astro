---
import { Image } from 'astro:assets';
import banner from '@/src/assets/img/home/home-about.png';
import UniversalWrapper from '../Layout/UniversalWrapper.astro';
---

<UniversalWrapper>
  <h1>About <span>HR STARTIFY</span></h1>
  <section class="content">
    <Image
      src={banner}
      alt="Two people talking and working on two laptops."
      quality={80}
    />
    <article>
      <h2>WELCOME TO STARTIFY</h2>
      <p>
        Your Partner of passionate people & culture professionals who design
        powerful human resources experience for you.
      </p>
      <p>
        We work with small-medium organizations who want to get the people piece
        right.
      </p>
    </article>
  </section>
</UniversalWrapper>
<style lang="scss">
  h1 {
    margin-bottom: 1rem;
    text-align: center;
  }
  span {
    color: var(--text-color-secondary);
  }
  .content {
    display: grid;
    gap: var(--common-gap-mobile);
    img {
      border-radius: 1.25rem;
    }
    h2 {
      color: var(--text-color-secondary);
    }
    p:nth-of-type(2) {
      margin-top: var(--common-gap-mobile);
    }
  }

  @media (min-width: 768px) {
    h1 {
      margin-bottom: calc(var(--common-gap-mobile) + 1rem);
      font-size: 2.2rem;
    }
    .content {
      grid-template-columns: 1.7fr 1fr;
      article {
        place-self: center;
        h2 {
          margin-bottom: 1rem;
        }
      }
    }
  }
</style>
